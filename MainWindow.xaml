<Window x:Class="IndustrialHmiDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:IndustrialHmiDemo.ViewModels"
        mc:Ignorable="d"
        Title="工业上位机设备采集与监控 Demo"
        Height="760"
        MinHeight="680"
        Width="1180"
        MinWidth="1060"
        Background="#F4F7FB"
        FontFamily="Microsoft YaHei">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#172033" />
        </Style>
        <Style x:Key="Panel" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#DCE5F2" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="14" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="0,0,8,8" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Background" Value="#2563EB" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#1D4ED8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="SecondaryButton" BasedOn="{StaticResource PrimaryButton}" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#1E3A8A" />
            <Setter Property="BorderBrush" Value="#BBD0F9" />
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#10233F" CornerRadius="8" Padding="18" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="工业上位机设备采集与监控 Demo"
                               Foreground="White"
                               FontSize="23"
                               FontWeight="Bold" />
                    <TextBlock Text="C# / WPF / MVVM / 异步采集 / 告警日志 / CSV 导出 / 模拟设备通信"
                               Foreground="#CFE3FF"
                               FontSize="13"
                               Margin="0,6,0,0" />
                </StackPanel>
                <Border Grid.Column="1" Background="#1E3A5F" CornerRadius="6" Padding="12,8" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="10" Height="10" Fill="{Binding StatusBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBlock Text="{Binding ConnectionState}" Foreground="White" FontWeight="SemiBold" FontSize="13" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="310" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Border Style="{StaticResource Panel}" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Text="设备控制" FontSize="17" FontWeight="Bold" Margin="0,0,0,12" />
                        <TextBlock Text="设备地址" FontWeight="SemiBold" />
                        <TextBox Text="{Binding DeviceAddress, UpdateSourceTrigger=PropertyChanged}" Height="32" Margin="0,6,0,12" Padding="8,5" />
                        <TextBlock Text="采样间隔（毫秒）" FontWeight="SemiBold" />
                        <Slider Minimum="300" Maximum="2000" TickFrequency="100" IsSnapToTickEnabled="True" Value="{Binding SamplingIntervalMs}" Margin="0,6,0,2" />
                        <TextBlock Text="{Binding SamplingIntervalMs, StringFormat={}{0:F0} ms}" Foreground="#60708A" Margin="0,0,0,12" />
                        <WrapPanel>
                            <Button Content="连接设备" Style="{StaticResource PrimaryButton}" Command="{Binding ConnectCommand}" />
                            <Button Content="断开" Style="{StaticResource SecondaryButton}" Command="{Binding DisconnectCommand}" />
                            <Button Content="开始采集" Style="{StaticResource PrimaryButton}" Command="{Binding StartCollectCommand}" />
                            <Button Content="停止" Style="{StaticResource SecondaryButton}" Command="{Binding StopCollectCommand}" />
                            <Button Content="导出 CSV" Style="{StaticResource SecondaryButton}" Command="{Binding ExportCommand}" />
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Panel}">
                    <StackPanel>
                        <TextBlock Text="运行日志" FontSize="17" FontWeight="Bold" Margin="0,0,0,10" />
                        <ListBox ItemsSource="{Binding Logs}" Height="330" BorderBrush="#E4EBF5" FontSize="12" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="300" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <UniformGrid Grid.Row="0" Columns="5" Margin="0,0,0,14">
                    <Border Style="{StaticResource Panel}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="温度" Foreground="#60708A" />
                            <TextBlock Text="{Binding Temperature, StringFormat={}{0:F1} ℃}" FontSize="24" FontWeight="Bold" />
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource Panel}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="压力" Foreground="#60708A" />
                            <TextBlock Text="{Binding Pressure, StringFormat={}{0:F2} MPa}" FontSize="24" FontWeight="Bold" />
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource Panel}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="振动" Foreground="#60708A" />
                            <TextBlock Text="{Binding Vibration, StringFormat={}{0:F2} mm/s}" FontSize="24" FontWeight="Bold" />
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource Panel}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="转速" Foreground="#60708A" />
                            <TextBlock Text="{Binding Speed, StringFormat={}{0} rpm}" FontSize="24" FontWeight="Bold" />
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource Panel}">
                        <StackPanel>
                            <TextBlock Text="告警数" Foreground="#60708A" />
                            <TextBlock Text="{Binding AlarmCount}" FontSize="24" FontWeight="Bold" Foreground="#B91C1C" />
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <Border Grid.Row="1" Style="{StaticResource Panel}" Margin="0,0,0,14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="实时温度曲线" FontSize="17" FontWeight="Bold" Margin="0,0,0,8" />
                        <Viewbox Grid.Row="1" Stretch="Fill">
                            <Grid Width="760" Height="210">
                                <Rectangle Fill="#F8FAFC" Stroke="#DCE5F2" RadiusX="4" RadiusY="4" />
                                <Line X1="40" Y1="170" X2="735" Y2="170" Stroke="#CBD5E1" />
                                <Line X1="40" Y1="20" X2="40" Y2="170" Stroke="#CBD5E1" />
                                <Line X1="40" Y1="95" X2="735" Y2="95" Stroke="#E2E8F0" StrokeDashArray="5,5" />
                                <Polyline Points="{Binding TemperatureChartPoints}" Stroke="#2563EB" StrokeThickness="4" StrokeLineJoin="Round" />
                                <TextBlock Text="高" Margin="8,18,0,0" Foreground="#64748B" />
                                <TextBlock Text="低" Margin="8,158,0,0" Foreground="#64748B" />
                            </Grid>
                        </Viewbox>
                    </Grid>
                </Border>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="330" />
                    </Grid.ColumnDefinitions>

                    <Border Style="{StaticResource Panel}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="采集数据" FontSize="17" FontWeight="Bold" Margin="0,0,0,10" />
                            <DataGrid Grid.Row="1" ItemsSource="{Binding Samples}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" BorderBrush="#E4EBF5" GridLinesVisibility="Horizontal">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=HH:mm:ss}" Width="90" />
                                    <DataGridTextColumn Header="温度" Binding="{Binding Temperature, StringFormat={}{0:F1}}" Width="80" />
                                    <DataGridTextColumn Header="压力" Binding="{Binding Pressure, StringFormat={}{0:F2}}" Width="80" />
                                    <DataGridTextColumn Header="振动" Binding="{Binding Vibration, StringFormat={}{0:F2}}" Width="80" />
                                    <DataGridTextColumn Header="转速" Binding="{Binding Speed}" Width="80" />
                                    <DataGridTextColumn Header="运行" Binding="{Binding MotorState}" Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource Panel}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="告警记录" FontSize="17" FontWeight="Bold" Margin="0,0,0,10" />
                            <ListBox Grid.Row="1" ItemsSource="{Binding Alarms}" BorderBrush="#E4EBF5">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,5">
                                            <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="#B91C1C" />
                                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" FontSize="12" Foreground="#334155" />
                                            <TextBlock Text="{Binding CreatedAt, StringFormat=HH:mm:ss}" FontSize="11" Foreground="#64748B" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
